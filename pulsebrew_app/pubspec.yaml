name: pulsebrew_app
description: PulseBrew Mobile Application
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.2.0
  flutter_blue_plus: ^1.31.8
  health: ^13.0.0
  device_info_plus: ^10.1.0
  uuid: ^4.3.3
  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0 # Missed this earlier
  supabase_flutter: ^2.3.0
  connectivity_plus: ^5.0.0 # Pinning to 5.x for simpler API for now, or use ^6.0.0 and update code
  provider: ^6.1.1
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

